# ===================================================================
# EduLite Docker Environment Configuration
# ===================================================================
# Copy this file to .env and update values for your local setup
# Command: cp .env.example .env
# ===================================================================

# -------------------------------------------------------------------
# PostgreSQL Database Configuration
# -------------------------------------------------------------------
# Password for the PostgreSQL database
# SECURITY: Change this in production! Use a strong, random password.
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=edulite_dev_password_change_in_prod

# -------------------------------------------------------------------
# Django Secret Key
# -------------------------------------------------------------------
# Secret key for Django cryptographic signing
# SECURITY: Must be unique and kept secret! Never commit the actual key.
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION-use-get-random-secret-key

# -------------------------------------------------------------------
# Email Configuration (Optional - for sending emails)
# -------------------------------------------------------------------
# Leave empty to use Django's console email backend (prints to logs)
# For production, configure with your SMTP provider (Gmail, SendGrid, etc.)

# SMTP server hostname (e.g., smtp.gmail.com)
EMAIL_HOST=

# SMTP server port (usually 587 for TLS, 465 for SSL)
EMAIL_PORT=587

# Use TLS encryption (recommended)
EMAIL_USE_TLS=True

# SMTP username (usually your email address)
EMAIL_HOST_USER=

# SMTP password or app-specific password
EMAIL_HOST_PASSWORD=

# Default "From" address for outgoing emails
DEFAULT_FROM_EMAIL=EduLite <noreply@edulite.local>

# ===================================================================
# Additional Notes
# ===================================================================
#
# Database Connection:
# - The backend automatically connects to PostgreSQL using DATABASE_URL
# - Format: postgresql://edulite_user:POSTGRES_PASSWORD@postgres:5432/edulite
# - No need to configure this manually
#
# Redis Connection:
# - Redis is used for Django Channels (WebSocket support)
# - Backend automatically connects to redis:6379
# - No additional configuration needed
#
# Frontend Configuration:
# - Frontend environment variables are in docker-compose.yml
# - API URL: http://localhost:8000/api
# - WebSocket URL: ws://localhost:8000/ws
#
# Security Best Practices:
# 1. Never commit .env to version control (it's in .gitignore)
# 2. Use strong, unique passwords for POSTGRES_PASSWORD
# 3. Generate a new DJANGO_SECRET_KEY for each environment
# 4. In production, set DEBUG=False in docker-compose.yml
# 5. Rotate secrets regularly
#
# ===================================================================
